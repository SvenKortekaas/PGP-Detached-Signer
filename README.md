# PGP-Detached-Signer
It's an JAVA program that creates an PGP armor detached sign for every file that is in the same directory as the .jar file

It works only on Windows with CMD.exe and GPG installed on your system